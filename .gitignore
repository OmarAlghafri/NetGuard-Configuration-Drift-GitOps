# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Secrets / real inventory - never commit real credentials
devices.yaml
*.secret
.env

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# Runtime / temp
*.tmp
*.log

# Local planning notes (not part of the published project)
network-projects-roadmap.md

# Transient run outputs
snapshots/
reports/*.md

# Editor workspace and raw/unsorted screenshots
docs/.obsidian/
_raw_screenshots/

# Real inventory captured device snapshots are not committed; baselines are.

# GNS3 project working directory (topology is built in the GUI; its runtime
# artifacts are machine-specific and not versioned inside the code repo).
# The topology .gns3 file can be exported into diagrams/ later for reference.
NetGuard*Engine/

